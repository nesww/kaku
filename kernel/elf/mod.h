#pragma once

#include <stdint.h>

/*===========================================================================
 * elf — ELF binary loader public API
 *
 * single module, no selective includes needed.
 * internal functions already use elf_* naming, exposed directly.
 *===========================================================================*/

#include "src/elf.h"
