#pragma once

#include <stdarg.h>

/*===========================================================================
 * log — logging public API
 *
 * single module, no selective includes needed.
 * internal functions already use log_* naming, exposed directly.
 *===========================================================================*/

#include "src/log.h"
